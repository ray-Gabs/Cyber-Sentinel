[tool.pip-audit]
# Accepted risks for packages that cannot be upgraded
# Add CVE IDs here with justification if new unfixable issues arise

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
exclude = ["venv", ".venv", "__pycache__"]
