server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — all routes served by index.html
    # /api and /ws are routed by the outer nginx reverse proxy, not here
    location / {
        try_files $uri $uri/ /index.html;
    }
}
