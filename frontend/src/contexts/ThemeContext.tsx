/**
 * ThemeContext — re-exports from the canonical provider location.
 * All new code should import from "@/providers/ThemeProvider" directly.
 */
export { ThemeProvider, useTheme } from "@/providers/ThemeProvider";
export type { Theme, Palette } from "@/providers/ThemeProvider";
